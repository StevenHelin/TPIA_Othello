//Coder une fonction d'evaluation
// implement min max, l'evoluer en alpha beta
//ne pas oublier d implement la liste chainée

int evaluation(noeud n){}; //a coder va servir a check les cases de score du tableau pour le meilleur coup possible
int estterminal(noeud n){};//va chercher si le noeud est terminal ou nom retourne 1 si oui , 0 sinon
int etendre(noeud n){};
int maxValue(noeud n){
    int v=0;
    if(estterminal(n)){
        return evaluation(n);
    }
    v = -1;
    for n2
};
int minValue(noeud n){};